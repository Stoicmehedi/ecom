# Turn the "engines" field in package.json into a hard install-time error
# instead of a warning npm prints and ignores. Without this, a machine on the
# wrong Node version installs cleanly and then fails later at build or run time
# with an error that never mentions Node.
engine-strict=true
